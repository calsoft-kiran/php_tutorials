<form method="post" action="{{ path('app_aircraft_details_delete', {'id': aircraft_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aircraft_detail.id) }}">
    <button class="btn">Delete</button>
</form>
