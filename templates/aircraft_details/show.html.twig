{% extends 'base.html.twig' %}

{% block title %}AircraftDetails{% endblock %}

{% block body %}
    <h1>AircraftDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aircraft_detail.id }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ aircraft_detail.details }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_aircraft_details_index') }}">back to list</a>

    <a href="{{ path('app_aircraft_details_edit', {'id': aircraft_detail.id}) }}">edit</a>

    {{ include('aircraft_details/_delete_form.html.twig') }}
{% endblock %}
